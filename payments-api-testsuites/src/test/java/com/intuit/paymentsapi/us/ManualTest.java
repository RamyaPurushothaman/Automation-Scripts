package com.intuit.paymentsapi.us;

public class ManualTest {
	
	/**
	 * If DBA is null, log warning and abort (invalid merchant account error).
If business street is null, log warning and abort (invalid merchant account error).
If business city is null, log warning and abort (invalid merchant account error).
If business state is null, log warning and abort (invalid merchant account error).
If business zip is null, log warning and abort (invalid merchant account error).
If business country is null or is not 3 characters, log warning and abort (invalid merchant account error).
If MCC is null or not 4 characters, log warning and abort (invalid merchant account error).
If business phone is null or not 10 characters, log warning and abort (invalid merchant account error).
	 * 
	 * 
	 */

}
